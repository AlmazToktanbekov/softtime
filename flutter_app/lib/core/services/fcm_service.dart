// FCM stub — Firebase не подключён.
// Для активации push-уведомлений добавьте GoogleService-Info.plist (iOS)
// и google-services.json (Android) из Firebase Console, затем раскомментируйте
// пакеты firebase_core и firebase_messaging в pubspec.yaml.
class FcmService {
  static Future<void> init() async {}
  static void dispose() {}
}
